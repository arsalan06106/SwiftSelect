if (!window.SwiftSelect) window.SwiftSelect = {};

if (!window.SwiftSelect.styles) {
  window.SwiftSelect.styles = {
    // CSS has been moved to styles.css and is loaded via fetch() in ui.js
    SHADOW_CSS: ``,
  };
}
